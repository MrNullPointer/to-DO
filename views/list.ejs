<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List</title>
  </head>
  <body>
    <% if (kindOfDay === "Saturday"){ %>
    <h1 style="color: blueviolet"><%=kindOfDay%></h1>
    <% } else{ %>
    <h1 style="color: brown"><%= kindOfDay %></h1>
    <% } %>
  </body>
</html>
